The "N Queens" problem solver in Python.

You can run the program by typing

```
python3 nqueens.py <BOARD_SIZE>
```

in terminal, while substituting `<BOARD_SIZE>` with some sensible positive integer.
